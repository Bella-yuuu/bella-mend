name: Scan

on:
  push:
    branches:
      - main

jobs:
  call-mend-scan:
    uses: Bella-yuuu/bella-mend/.github/workflows/template.yml@main
    with:
      PRODUCT_NAME: "Bella_Github"
      PROJECT_NAME: "0725"
    secrets:
